{
  "config": {
    "step": {
      "user": {
        "title": "Network Scanner Extended",
        "description": "Choose a presence provider (OPNsense ARP or AdGuard DHCP), optionally enable UniFi enrichment, and add directory overlay settings."
      },
      "opnsense": {
        "title": "OPNsense presence provider"
      },
      "adguard": {
        "title": "AdGuard presence provider"
      },
      "unifi": {
        "title": "UniFi enrichment"
      }
    },
    "error": {
      "opnsense_url_required": "OPNsense URL is required.",
      "opnsense_creds_required": "OPNsense key + secret are required.",
      "adguard_url_required": "AdGuard URL is required.",
      "unifi_url_required": "UniFi URL is required.",
      "unifi_auth_required": "Provide a UniFi token or username + password."
    }
  }
}
